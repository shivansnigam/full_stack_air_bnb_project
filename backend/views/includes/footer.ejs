<footer>
    <style>
        footer {
            background-color: #1a1a1a; /* Dark background color */
            color: white; /* Text color */
            padding: 20px 0; /* Padding around the footer */
            text-align: center;
            margin-top: 10px; /* Center text */
        }

        .f-info {
            max-width: 1200px; /* Max width for larger screens */
            margin: 0 auto; /* Center the footer */
            padding: 0 15px; /* Side padding */
        }

        .f-info-socials {
            margin-bottom: 15px; /* Space below social icons */
        }

        .social-icon {
            color: white; /* Icon color */
            margin: 0 10px; /* Space between icons */
            font-size: 24px; /* Size of icons */
            transition: color 0.3s ease; /* Transition for hover effect */
        }

        .social-icon:hover {
            color: #3498db; /* Change color on hover */
        }

        .f-info-brand {
            margin-bottom: 10px; /* Space below brand text */
            font-weight: bold; /* Bold text */
        }

        .f-info-links {
            margin-top: 10px; /* Space above links */
        }

        .f-info-links a {
            color: white; /* Link color */
            margin: 0 15px; /* Space between links */
            text-decoration: none; /* No underline */
            transition: color 0.3s ease; /* Transition for hover effect */
        }

        .f-info-links a:hover {
            color: #3498db; /* Change link color on hover */
        }

        /* Responsive design */
        @media (max-width: 768px) {
            .f-info {
                padding: 0 10px; /* Reduced padding for smaller screens */
            }

            .social-icon {
                margin: 0 5px; /* Reduce spacing on mobile */
                font-size: 20px; /* Smaller icons on mobile */
            }

            .f-info-links a {
                margin: 0 10px; /* Reduce spacing on mobile */
            }
        }
    </style>
    <div class="f-info">
        <div class="f-info-socials">
            <a href="#" aria-label="Facebook" class="social-icon"><i class="fa-brands fa-facebook"></i></a>
            <a href="#" aria-label="Instagram" class="social-icon"><i class="fa-brands fa-square-instagram"></i></a>
            <a href="#" aria-label="LinkedIn" class="social-icon"><i class="fa-brands fa-linkedin"></i></a>
        </div>
        <div class="f-info-brand">&copy; Wanderlust Private Limited</div>
        <div class="f-info-links">
            <a href="/privacy">Privacy</a>
            <a href="/terms">Terms</a>
        </div>
    </div>
</footer>
